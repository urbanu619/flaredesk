{
  "name": "flaredesk-mcp",
  "version": "1.0.0",
  "description": "MCP Server for Cloudflare DNS batch management — multi-account, bulk operations, one-click proxy toggle",
  "main": "dist/index.js",
  "bin": {
    "flaredesk-mcp": "dist/index.js"
  },
  "files": [
    "dist/",
    "smithery.yaml",
    "README.md"
  ],
  "scripts": {
    "build": "tsc && chmod +x dist/index.js",
    "prepare": "npm run build",
    "dev": "ts-node src/index.ts",
    "start": "node dist/index.js"
  },
  "keywords": ["mcp", "cloudflare", "dns", "batch", "proxy", "dns-management"],
  "author": "urbanu619",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  }
}
